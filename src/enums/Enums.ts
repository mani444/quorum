export const ContentType = Object.freeze({
  json: "application/json",
  multipart: "multipart/form-data",
});

export const ECategoryType = Object.freeze({
  FEED: "FEED",
  LIVE: "LIVE",
  AFFINITY: "AFFINITY",
});
export const E_RSVP_Type = Object.freeze({
  YES: "yes",
  NO: "no",
});
export const E_GROUP_Type = Object.freeze({
  PUBLIC: "public",
  PRIVATE: "private",
});
export const C_CHAT_TYPE = Object.freeze({
  IMAGE: "image",
});
export const E_GENDER_Type = Object.freeze({
  MALE: "male",
  FEMALE: "female",
});
export const E_COLLECTIONS = Object.freeze({
  CATEGORIES: "categories",
  CHAT_ROOMS: "chatRooms",
  USERS: "Users",
});
